{
  "name": "laneways",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r --filter './packages/*' --filter './tools/*' test",
    "typecheck": "pnpm -r --filter './packages/*' --filter './tools/*' typecheck",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.65.0",
    "vitest": "^3.0.0"
  }
}
