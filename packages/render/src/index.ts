/**
 * Task 1 stub. `render`'s real exports (camera, atlas, drawFrame, palette,
 * the RenderFrame interface types) land in later M2 tasks — see the plan's
 * File structure table. `render` never imports from `@laneways/sim` or
 * `@laneways/shared`; see `test/boundary.test.ts`.
 */
export * from './types'
